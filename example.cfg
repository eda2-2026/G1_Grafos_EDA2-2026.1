# SeedForge search profile — hand-editable.
# Globals first, then one [filter] block per condition (all must pass).
mc = 1.21
dim = 0
seed_start = 0
seed_end = 20000000000
out = hits.txt
verbose = 1
gpus = 0,1

# Cheap: a mushroom-fields biome somewhere near origin
[filter]
kind = structure
biome = desert_pyramid
x = 0
z = 0
radius = 1000

# Spawn should land in plains or forest-ish (expensive, runs last automatically)
